<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only the launch window uses these; everything after the first frame is drawn by Compose from
      Palette.kt. They exist so the window behind the app does not flash white before a dark-themed
      first frame. Keep them in step with Palette.Light.background / Palette.Dark.background.
    -->
    <color name="fixhora_window_background">#FFFFFFFF</color>
</resources>
